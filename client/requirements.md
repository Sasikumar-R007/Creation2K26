## Packages
framer-motion | Complex page transitions and scroll animations
date-fns | Date formatting for schedules and events
react-particles | Background particle effects for tech theme
tsparticles-slim | Lightweight particle engine
lucide-react | Icons for UI elements (already in base, but confirming usage)
recharts | Dashboard analytics charts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
